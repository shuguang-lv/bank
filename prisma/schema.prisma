generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model BANK_USERS {
  EMAIL    String  @db.VarChar(72)
  PWD_HASH String  @db.VarChar(72)
  BALANCE  Decimal @db.Decimal(10, 0)
  USER_ID  String  @id @db.VarChar(36)
}
